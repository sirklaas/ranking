import type { FaseKey } from '@/types/fases';

export const key: FaseKey = '07/05';
export const group = '07';
export const title = 'Zitten en Staan';
export const skipTrailer = true;
export const needs = { media: false } as const;
